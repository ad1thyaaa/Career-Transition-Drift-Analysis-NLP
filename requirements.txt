# ============================================================
# Career Drift Trajectory Analysis — Python Dependencies
# ============================================================
# Install with:
#   pip install -r requirements.txt
#
# Tested on Python 3.10+.  Pin versions for fully reproducible
# environments; ranges are given as minimum-tested baselines.
# ============================================================

# ---- Core data / numerics ----------------------------------
numpy>=1.24
pandas>=2.0
pyarrow>=14.0          # parquet I/O backend for pandas

# ---- Machine learning --------------------------------------
scipy>=1.11
scikit-learn>=1.3
lightgbm>=4.0

# ---- NLP / sentence embeddings -----------------------------
sentence-transformers>=2.7
# sentence-transformers pulls torch and transformers transitively;
# for GPU support install torch separately before this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

# ---- Plotting ----------------------------------------------
matplotlib>=3.8
seaborn>=0.13

# ---- Persistence -------------------------------------------
joblib>=1.3            # model checkpointing (ships with scikit-learn)

# ---- Dev / optional QoL ------------------------------------
tqdm>=4.66             # progress bars used by sentence-transformers
